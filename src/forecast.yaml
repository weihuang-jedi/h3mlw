paths:
  data_pattern: "../data/global_h3_20years.zarr"
  checkpoint_path: "checkpoints/best-h3-20year-cyclic-model.ckpt"
  output_nc: "h3_autoregressive_forecast.nc"
  initial_condition_nc: "../data/global_h3_res2_air_sfc_2000.nc"

model_params:
  # CHANGE THIS FROM 4 TO 2 to match your model's trained weights
  history_steps: 2       
  forecast_offset: 1     
  steps_per_day: 8       

forecast_settings:
  forecast_start_time: "2000-01-01 00:00:00"
  forecast_days: 5
